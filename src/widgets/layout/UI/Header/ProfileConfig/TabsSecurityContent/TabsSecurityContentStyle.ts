import { Dialog } from "@radix-ui/themes";
import styled from "styled-components";

const DialogContentSC = styled(Dialog.Content)`
  max-width: 413px;
`;

export const TabsSecurityContentStyle = () => ({ DialogContentSC });
