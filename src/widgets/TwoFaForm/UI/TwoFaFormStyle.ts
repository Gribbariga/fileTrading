import { Card } from "@radix-ui/themes";
import styled from "styled-components";

const RegisterWrapperSC = styled(Card)`
  max-width: 400px;
  width: 100%;
`;

export const TwoFaFormStyle = () => ({ RegisterWrapperSC });
